int dados(int num_casilla)
{
int dado1, dado2,dadototal;

srand(time(NULL))

dado1=(rand() % 6)+1
printf("Has sacado un %d\n", dado1);

if(casilla>=60){
dado2=0;
}
else
{dado2=(rand() % 6)+1}
printf("Has sacado un %d\n", dado2);

dadototal=dado1+dado2;

printf("La suma de los dados es %d\n", dadototal);


return dadototal;  
}
